version: 0.2

phases:
  install:
    commands:
      - npm i
  build:
    commands:
      - npm run build
artifacts:
  files:
    - package.json
    - backend/dist/**/*
    - backend/package.json
    - frontend/dist/**/*
    - frontend/package.json
